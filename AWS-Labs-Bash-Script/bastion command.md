1. vi abc.pem (read text files)

2. escape press

3. shift :

4. wq(wq command in Vim is used to write changes to a file and quit the editor.)

5. chmod 400 abc.pem(used to modify file permissions.)

6. ssh -i abc.pem ec2-user@10.0.0.211